  on:
    pull_request:
      branches:
        - branch1
    push:
      branches:
        - branch1
  name: "Build and Release"
  jobs:
    build:
      name: Build and Release
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v1
        - uses: actions/setup-java@1
          with:
            java-version: '12.x'
        - uses: actions/flutter-action@v1
          with:
            flutter-version: '2.20'
        - run: flutter pub get
        - run: flutter test
        - run: flutter build apk --debug --split-per-abi
        - name: push to releases
          uses: ncipollo/release-action@1
          with:
            artifacts: "build/app/outputs/apk/debug/*"
            tag: v1.0.${{ github.run_number }}
            token: ${{ secrets.Token }}





  # push to branch1
# pull request on branch1
